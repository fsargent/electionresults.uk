name = "electionresults-uk"
pages_build_output_dir = ".svelte-kit/cloudflare"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Note: Cloudflare Pages does not yet support the [observability] block
# in wrangler.toml (Workers-only). To enable logs / traces for this
# Pages project, configure them in the Cloudflare dashboard:
#   Workers & Pages → electionresults-uk → Settings → Functions → Logs.
